import getRandomInt from '../functions/getRandomInt';
import mazeSettings from './mazeSettings';

const mazeWidth = mazeSettings.width;

const mainPlayerSettings = {
    playerX: 1,
    playerY: 1,
    playerIcon: 'img/player-icon.png'
}

export default mainPlayerSettings;